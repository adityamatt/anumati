{
  "name": "anumati",
  "version": "0.1.0",
  "description": "PreToolUse hook for Claude Code — config-driven allow/deny rules to reduce permission prompts",
  "main": "dist/index.js",
  "bin": {
    "anumati": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "dev": "ts-node src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0",
    "vitest": "^4.1.9"
  },
  "keywords": [
    "claude",
    "claude-code",
    "hooks",
    "permissions",
    "anthropic"
  ],
  "license": "MIT",
  "engines": {
    "node": ">=18"
  }
}
